import NodeLogger from './logger/node';
/**
 * Sets the logger instance. For internal use only.
 */
export declare function setInstance(newInstance: NodeLogger): void;
/**
 * Returns the current logger instance.
 * @deprecated
 *
 * In TypeScript, use default import:
 * ```
 *  import Logger from './Logger';
 *  Logger().info(...)
 * ```
 *
 * In JavaScript, use:
 * ```
 *  const Logger = require('./Logger').default;
 *  Logger().info(...)
 * ```
 */
export declare function getInstance(): NodeLogger;
declare const _default: () => NodeLogger;
export default _default;
